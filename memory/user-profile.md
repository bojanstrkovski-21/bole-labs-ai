---
name: user-profile
description: Who the user is, their environment, and their tool stack — merged from multiple projects
metadata:
  type: user
---

Bojan Strkovski (bojanstrkovski.21@gmail.com). Runs Arch Linux as primary OS
(boki setup). Works in Macedonian timezone (Europe/Skopje).

Maintains a custom Arch Linux installer called archknife and a personal
package repo (shtrkce-repo, shtrkce_repo_xl hosted on GitHub Pages/GitLab).
Has an ArcoLinux background (knows Nemesis repo, Erik Dubois' tooling).
Comfortable reading and directing code changes but prefers to be explained
what something does before changes are applied — asks "what is it" before
"fix it."

Tool stack: nvim, kitty terminal, starship prompt, eza (ls replacement),
zoxide, fzf, carapace completions, NVM, Rust/cargo, paru (AUR helper). Has
xmonad, hyprland, and chadwm setups. Runs a multi-distro portable bash config
project (arch/debian/fedora/opensuse/void alias files per distro), storing
aliases in `~/.config/bash_aliases/` with one file per distro.

[[feedback-working-style]]
